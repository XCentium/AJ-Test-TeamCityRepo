@using Insite.Account.Content
<div class="large-6 columns si-acct">
    <h1>@T("Register")</h1>
    <div class="cm" ng-include="'register_createAccountContent'"></div>
    <div class="buttons">
        <a id="registerCreateAccountButton" class="button tertiary" ng-href="@(Url.ContentPage<CreateAccountPage>())?returnUrl={{vm.returnUrl}}">@T("Register")</a>
    </div>
</div>