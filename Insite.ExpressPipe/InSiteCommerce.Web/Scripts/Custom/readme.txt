Custom javascript files in this folder and subfolders will be added to the global.min.js bundle, after the default scripts.

Add files to custom.xml if you need them to be included before the others (for base classes for example).